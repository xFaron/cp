#include <iostream>
#include <vector>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(nullptr); cout.tie(nullptr);

    int t;
    cin >> t;

    while (t--) {
        int n;
        cin >> n;

        vector<int> liar_count;
        vector<bool> liar;
        int count;

        int currIndex = 0;
        while (currIndex > 0) {
            if (liar_count[currIndex - 1])


            currIndex--;
        }
    }

    return 0;
}